Title:
Test issue — API Documentation Generator agent target

Body:
This issue exists purely as a target for testing the **API Documentation Generator V2** agent (`foundational/api-doc-generator-v2`). The agent posts its generated documentation as a comment on this issue's `prNumber`/`issueNumber` field.

Safe to close or delete once testing is done.

Testing: `src/routes/orderRoutes.js`
